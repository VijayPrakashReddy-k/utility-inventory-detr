# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/
.venv

# Conda
.conda/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/
*.ipynb

# Only push files needed for Streamlit deployment
# Everything else stays local

# Training code - NOW INCLUDED (useful for reproducibility)
# training/ - Allow training scripts and configs
# But exclude large log files
training/logs/*.log
training/logs/*.txt
# Allow training_log.json (used by Streamlit About page)
!training/logs/training_log.json

# Data processing scripts - NOW INCLUDED (useful for reproducibility)
# scripts/ - Allow scripts to be committed

# Datasets - Include metadata AND images (via Git LFS)
datasets/raw/
# Allow merged dataset files (JSON metadata + images via LFS)
!datasets/processed/merged/
# Allow dataset README files
!datasets/**/README*.txt
!datasets/**/README*.md

# Model checkpoints (not needed, only best_model.pth is needed)
models/checkpoints/
models/final/*.pth
!models/final/best_model.pth

# Training setup files - NOW INCLUDED (useful for reproducibility)
# requirements-training.txt - Allow requirements file
# setup_conda_env.sh - Allow setup script

# Empty directories
notebooks/

# Temporary files
*.tmp
*.temp
*.bak

# Streamlit secrets
.streamlit/secrets.toml

